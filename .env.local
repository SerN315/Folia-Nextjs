# Points Next.js rewrites to the PersonalHub backend.
# In production set this to your hosted backend URL (same as PersonalHub's BACKEND_URL).
# The browser never sees this value — all /api/* calls are proxied server-side.
BACKEND_URL=http://localhost:5000
